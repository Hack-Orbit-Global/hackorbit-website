{
  "name": "hackorbit-website",
  "version": "1.0.0",
  "private": true,
  "description": "Hack Orbit — official open-source technology organisation website. Plain HTML5/CSS3/Vanilla JS + Vercel Serverless Functions + Google Sheets/Apps Script.",
  "type": "module",
  "scripts": {
    "build": "node frontend/build.js && node seo/sitemap-generate.js",
    "dev": "vercel dev",
    "start": "vercel dev",
    "test": "node --test tests/",
    "test:build": "node frontend/build.js && node --test tests/build-structure.test.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
